# SPDX-FileCopyrightText: Copyright (c) 2025 Cisco and/or its affiliates.
# SPDX-License-Identifier: Apache-2.0

# TODO(msardara): Add a test to check if the connection to the service is working
# apiVersion: v1
# kind: Pod
# metadata:
#   name: "{{ include "agp.fullname" . }}-test-connection"
#   labels:
#     {{- include "agp.labels" . | nindent 4 }}
#   annotations:
#     "helm.sh/hook": test
# spec:
#   containers:
#     - name: wget
#       image: busybox
#       command: ['wget']
#       args: ['{{ include "agp.fullname" . }}:{{ .Values.service.port }}']
#   restartPolicy: Never
