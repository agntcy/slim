apiVersion: v2
name: spire-downstream
description: Downstream SPIRE (Kind) — joins root SPIRE on cluster-a via join token
type: application
version: "0.1.0"
appVersion: "1.14.2"
dependencies:
  - name: spire
    version: "0.28.3"
    repository: file://./charts/spire