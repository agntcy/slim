version: '3'

# Taskfile for slimctl
# Run from control-plane/slimctl directory or use: task -d control-plane/slimctl <task-name>

vars:
  # Binary location relative to this Taskfile (control-plane/slimctl/)
  SLIMCTL_BIN: ../../.dist/bin/slimctl
  TESTDATA_DIR: testdata
  TEST_CERTS_DIR: testdata/certs

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list
    silent: true

  help:manual:
    desc: Show manual testing help
    cmds:
      - echo "╔════════════════════════════════════════════════════╗"
      - echo "║     SLIM Manual Testing Guide                     ║"
      - echo "╚════════════════════════════════════════════════════╝"
      - echo ""
      - echo "🎯 Quick Start:"
      - echo "  task test:manual           - Complete guided workflow"
      - echo "  task run:base              - Run with base config"
      - echo "  task run:tls               - Run with TLS config"
      - echo ""
      - echo "📋 Step-by-step:"
      - echo "  1. Build binary            - cd ../.. && task -d control-plane slimctl:build"
      - echo "  2. task test:manual        - Run manual test"
      - echo ""
      - echo "🔧 Custom testing:"
      - echo "  task run:config CONFIG=path/to/config.yaml"
      - echo ""
      - echo "🧹 Cleanup:"
      - echo "  task clean                 - Remove test files"
      - echo ""
      - echo "For full command list - task --list"
    silent: true

  # Testing tasks (manual testing only)
  test:
    desc: Run manual tests (use test:manual for guided workflow)
    cmds:
      - task: test:manual

  test:manual:
    desc: Complete manual testing workflow
    cmds:
      - echo "╔════════════════════════════════════════════════════╗"
      - echo "║     SLIM Manual Testing Workflow                  ║"
      - echo "╚════════════════════════════════════════════════════╝"
      - echo ""
      - echo "📋 Step 1/3 - Verifying binary..."
      - task: verify:binary
      - echo ""
      - echo "📋 Step 2/3 - Environment ready!"
      - echo ""
      - echo "Available test scenarios:"
      - echo "  1️⃣  Base config - task run:base"
      - echo "  2️⃣  TLS config - task run:tls"
      - echo "  3️⃣  Custom config - task run:config CONFIG=path/to/config.yaml"
      - echo ""
      - echo "Or run directly:"
      - echo "  {{.SLIMCTL_BIN}} slim start --config ../../data-plane/config/base/server-config.yaml"
      - echo "  {{.SLIMCTL_BIN}} slim start --config ../../data-plane/config/base/server-config.yaml --endpoint 127.0.0.1:9090"
      - echo ""
      - echo "📋 Step 3/3 - Choose a test scenario above and run it! 🚀"
    silent: false

  # Certificate generation tasks (for TLS testing)
  certs:setup:
    desc: Generate test TLS certificates (X.509 v3)
    cmds:
      - task: certs:clean
      - task: certs:generate
      - task: certs:verify
    silent: false

  certs:generate:
    desc: Generate X.509 v3 certificates
    cmds:
      - echo "🔐 Generating test certificates..."
      - mkdir -p {{.TEST_CERTS_DIR}}
      - |
        # Generate private key
        openssl genrsa -out {{.TEST_CERTS_DIR}}/server-key.pem 2048 2>/dev/null
      - |
        # Create OpenSSL config for v3 extensions
        cat > {{.TEST_CERTS_DIR}}/openssl.cnf <<'EOF'
        [req]
        default_bits = 2048
        prompt = no
        default_md = sha256
        distinguished_name = dn
        x509_extensions = v3_req

        [dn]
        C = US
        ST = Test
        L = Test
        O = Test Organization
        CN = localhost

        [v3_req]
        subjectAltName = @alt_names
        basicConstraints = CA:FALSE
        keyUsage = digitalSignature, keyEncipherment
        extendedKeyUsage = serverAuth

        [alt_names]
        DNS.1 = localhost
        DNS.2 = *.localhost
        IP.1 = 127.0.0.1
        IP.2 = ::1
        EOF
        
        # Generate X.509 v3 certificate
        openssl req -new -x509 -key {{.TEST_CERTS_DIR}}/server-key.pem \
          -out {{.TEST_CERTS_DIR}}/server-cert.pem -days 365 \
          -config {{.TEST_CERTS_DIR}}/openssl.cnf \
          -extensions v3_req 2>/dev/null
        
        # Clean up config file
        rm {{.TEST_CERTS_DIR}}/openssl.cnf
        
        echo "✅ Certificates generated in {{.TEST_CERTS_DIR}}/"
    silent: false
    status:
      - test -f {{.TEST_CERTS_DIR}}/server-cert.pem
      - test -f {{.TEST_CERTS_DIR}}/server-key.pem

  certs:verify:
    desc: Verify certificate version and details
    cmds:
      - |
        echo "🔍 Verifying certificate..."
        CERT_VERSION=$(openssl x509 -in {{.TEST_CERTS_DIR}}/server-cert.pem -noout -text | grep "Version:" | sed 's/.*Version: \([0-9]\).*/\1/')
        if [ "$CERT_VERSION" = "3" ]; then
          echo "   ✅ Certificate version: v3 (compatible with rustls)"
        else
          echo "   ❌ Warning: Certificate version v$CERT_VERSION may not be compatible"
          exit 1
        fi
      - echo "   Certificate details:"
      - openssl x509 -in {{.TEST_CERTS_DIR}}/server-cert.pem -noout -subject -dates
    silent: false
    preconditions:
      - test -f {{.TEST_CERTS_DIR}}/server-cert.pem

  certs:clean:
    desc: Remove test certificates
    cmds:
      - echo "🧹 Cleaning test certificates..."
      - rm -rf {{.TEST_CERTS_DIR}}
      - echo "✅ Certificates removed"
    silent: false

  certs:info:
    desc: Show certificate information
    cmds:
      - echo "📜 Certificate Information:"
      - openssl x509 -in {{.TEST_CERTS_DIR}}/server-cert.pem -noout -text
    silent: false
    preconditions:
      - test -f {{.TEST_CERTS_DIR}}/server-cert.pem

  # Run tasks (for quick testing)
  run:base:
    desc: Run slimctl with base configuration
    cmds:
      - echo "🚀 Starting slimctl with base config..."
      - echo "   Config: ../../data-plane/config/base/server-config.yaml"
      - echo ""
      - "{{.SLIMCTL_BIN}} slim start --config ../../data-plane/config/base/server-config.yaml"
    silent: false
    preconditions:
      - sh: test -f {{.SLIMCTL_BIN}}
        msg: "slimctl binary not found at {{.SLIMCTL_BIN}}. Build it first with: cd ../.. && task -d control-plane slimctl:build"
      - sh: test -f ../../data-plane/config/base/server-config.yaml
        msg: "Base config not found at ../../data-plane/config/base/server-config.yaml"

  run:tls:
    desc: Run slimctl with TLS configuration
    cmds:
      - echo "🚀 Starting slimctl with TLS config..."
      - echo "   Config: ../../data-plane/config/tls/server-config.yaml"
      - echo ""
      - "{{.SLIMCTL_BIN}} slim start --config ../../data-plane/config/tls/server-config.yaml"
    silent: false
    preconditions:
      - sh: test -f {{.SLIMCTL_BIN}}
        msg: "slimctl binary not found at {{.SLIMCTL_BIN}}. Build it first with: cd ../.. && task -d control-plane slimctl:build"
      - sh: test -f ../../data-plane/config/tls/server-config.yaml
        msg: "TLS config not found at ../../data-plane/config/tls/server-config.yaml"

  run:config:
    desc: Run slimctl with custom config file
    cmds:
      - echo "🚀 Starting slimctl with config: {{.CONFIG}}"
      - "{{.SLIMCTL_BIN}} slim start --config {{.CONFIG}}"
    silent: false
    requires:
      vars: [CONFIG]
    preconditions:
      - sh: test -f {{.SLIMCTL_BIN}}
        msg: "slimctl binary not found at {{.SLIMCTL_BIN}}. Build it first with: cd ../.. && task -d control-plane slimctl:build"
      - sh: test -f {{.CONFIG}}
        msg: "Config file not found: {{.CONFIG}}"

  run:endpoint-override:
    desc: Test endpoint override with CLI flag
    cmds:
      - echo "🧪 Testing endpoint override..."
      - echo "   Config: ../../data-plane/config/base/server-config.yaml"
      - echo "   Override: --endpoint 127.0.0.1:9090"
      - echo ""
      - "{{.SLIMCTL_BIN}} slim start --config ../../data-plane/config/base/server-config.yaml --endpoint 127.0.0.1:9090"
    silent: false
    preconditions:
      - sh: test -f {{.SLIMCTL_BIN}}
        msg: "slimctl binary not found at {{.SLIMCTL_BIN}}. Build it first with: cd ../.. && task -d control-plane slimctl:build"

  # Clean tasks
  clean:
    desc: Clean all generated test files
    cmds:
      - echo "🧹 Cleaning generated files..."
      - task: certs:clean
      - rm -rf {{.TESTDATA_DIR}}
      - echo "✅ Clean complete"
    silent: false

  clean:all:
    desc: Clean everything including binary
    cmds:
      - task: clean
      - rm -f {{.SLIMCTL_BIN}}
      - echo "✅ All clean (including binary at {{.SLIMCTL_BIN}})"
    silent: false

  # Validation tasks
  validate:
    desc: Validate test environment setup
    cmds:
      - echo "🔍 Validating test environment..."
      - task: verify:binary
      - echo "✅ Validation complete"
    silent: false

  verify:binary:
    desc: Verify slimctl binary exists and show version
    cmds:
      - |
        if [ ! -f "{{.SLIMCTL_BIN}}" ]; then
          echo "❌ Binary not found at: {{.SLIMCTL_BIN}}"
          echo ""
          echo "Build it with:"
          echo "  cd ../.. && task -d control-plane slimctl:build"
          exit 1
        fi
        echo "✅ Binary found: {{.SLIMCTL_BIN}}"
        echo ""
        echo "Version info:"
        {{.SLIMCTL_BIN}} version 2>/dev/null || echo "  (version command not available)"
    silent: false
