[package]
name = "agntcy-slim-datapath"
version = "0.11.4"
edition = { workspace = true }
license = { workspace = true }
description = "Core data plane functionality for SLIM"

[lib]
name = "slim_datapath"

[features]
default = ["native"]
native = [
    "dep:agntcy-slim-config",
    "dep:agntcy-slim-tracing",
    "dep:display-error-chain",
    "dep:drain",
    "dep:fastwebsockets",
    "dep:h2",
    "dep:opentelemetry",
    "dep:tokio",
    "dep:tokio-stream",
    "dep:tokio-util",
    "dep:tonic",
    "dep:tonic-prost",
    "dep:tracing-opentelemetry",
]
wasm = [
    "dep:getrandom",
]

[dependencies]
# Always available (core types, messages, tables)
bincode = { workspace = true }
bit-vec = { workspace = true }
getrandom = { version = "0.3", features = ["wasm_js"], optional = true }
http = { workspace = true }
parking_lot = { workspace = true }
prost = { workspace = true }
rand = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
twox-hash = { workspace = true }

# Native-only dependencies
agntcy-slim-config = { workspace = true, optional = true, features = ["native"] }
agntcy-slim-tracing = { workspace = true, optional = true, features = ["native"] }
display-error-chain = { workspace = true, optional = true }
drain = { workspace = true, optional = true }
fastwebsockets = { workspace = true, optional = true }
h2 = { workspace = true, optional = true }
opentelemetry = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }
tokio-stream = { workspace = true, optional = true }
tokio-util = { workspace = true, optional = true }
tonic = { workspace = true, optional = true }
tonic-prost = { workspace = true, optional = true }
tracing-opentelemetry = { workspace = true, optional = true }

[build-dependencies]
protoc-bin-vendored = { workspace = true }
tonic-prost-build = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }
tracing-test = { workspace = true, features = ["no-env-filter"] }

[[bench]]
name = "pool_benchmark"
harness = false
