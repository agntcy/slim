// This go.mod is for local development only.
// When these files are copied to the target repository (agntcy/slim-bindings-go),
// they will use the target repo's go.mod instead.

module github.com/agntcy/slim-bindings-go

go 1.22

