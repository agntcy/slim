[project]
name = "slima2a"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10, <4.0"
dependencies = []

[tool.uv.sources]
srpc = { workspace = true }

[tool.ruff.lint.per-file-ignores]
"slima2a/types" = ["UP"]
