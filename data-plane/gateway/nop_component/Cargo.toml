[package]
name = "gateway-nop-component"
version = "0.1.0"
publish = false
edition = "2021"

[dependencies]
gateway-component = { path = "../component" }
gateway-config-grpc = { path = "../config/grpc" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_yaml = "0.9.34"
