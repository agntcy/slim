[package]
name = "gateway-config-tests-grpc"
version = "0.1.0"
edition = "2021"
description = """
TLS configuration utilities.
"""

[dependencies]
agp-gw-config-auth = { path = "../../auth" }
agp-gw-config-grpc = { path = "../../grpc" }
agp-gw-config-tls = { path = "../../tls" }
rustls = "0.23.20"
tokio = "1.42.0"
tonic = { version = "0.12" }
tracing = "0.1.41"
tracing-test = "0.2.5"

[build-dependencies]
tonic-build = { version = "0.12.3", features = ["prost"] }
