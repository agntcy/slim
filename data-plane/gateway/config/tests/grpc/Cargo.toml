[package]
name = "gateway-config-tests-grpc"
version = "0.1.0"
publish = false
edition = "2021"
description = """
TLS configuration utilities.
"""

[dependencies]
gateway-config-auth = { path = "../../auth" }
gateway-config-grpc = { path = "../../grpc" }
gateway-config-tls = { path = "../../tls" }
rustls = "0.23.20"
tokio = "1.42.0"
tonic = { version = "0.12" }
tracing = "0.1.41"
tracing-test = "0.2.5"

[build-dependencies]
tonic-build = { version = "0.12.3", features = ["prost"] }
