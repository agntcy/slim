name: Publish Comprehensive Coverage to Codecov

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  coverage:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: data-plane
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          workspace: ./data-plans

      - name: Install just
        run: |
          sudo snap install just --classic || true

      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov

      - name: Setup UV
        id: setup-uv
        uses: ./.github/actions/setup-python
        with:
          uv-install: true

      - name: Generate coverage report (Rust + Python)
        run: task data-plane:coverage-full

      # Run Go tests and generate coverage for control-plane
      - name: Run Go tests for control-plane coverage
        run: go test -coverprofile=coverage.out ./...
        working-directory: control-plane/control-plane
      # Upload combined coverage to Codecov
      - name: Upload combined coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: data-plane/lcov.info,data-plane/python/bindings/coverage.lcov,data-plane/python/integrations/slimrpc/coverage.lcov,control-plane/control-plane/coverage.out
          flags: rust,python,go
          name: combined-coverage
          fail_ci_if_error: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
