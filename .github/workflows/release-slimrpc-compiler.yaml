# Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

---
name: ci-release-slimrpc-compiler

on:
  push:
    tags:
      - 'protoc-gen-slimrpc-v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  ensure-release:
    name: ensure-slimrpc-compiler-release
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "${{ github.ref_name }}"

  build-binaries:
    name: "${{ matrix.platform.target }}"
    needs:
      - ensure-release
    runs-on: ${{ matrix.platform.runner }}
    permissions:
      contents: write
    defaults:
      run:
        shell: bash
        working-directory: ./data-plane/slimrpc-compiler
    strategy:
      fail-fast: true
      matrix:
        platform:
          - runner: ubuntu-24.04
            os: linux
            target: x86_64-unknown-linux-musl
          - runner: ubuntu-24.04
            os: linux
            target: aarch64-unknown-linux-musl
          - runner: windows-latest
            os: windows
            target: x86_64-pc-windows-msvc
          - runner: ubuntu-24.04
            os: linux
            target: x86_64-pc-windows-gnu
          - runner: windows-11-arm
            os: windows
            target: aarch64-pc-windows-msvc
          - runner: macos-15-intel
            os: macos
            target: x86_64-apple-darwin
          - runner: macos-15
            os: macos
            target: aarch64-apple-darwin
        exclude:
          - platform:
              exclude: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          workspace: ./data-plane

      - uses: taiki-e/upload-rust-binary-action@v1
        with:
          bin: protoc-gen-slimrpc-python
          target: ${{ matrix.platform.target }}
          tar: unix
          zip: windows
          token: ${{ secrets.GITHUB_TOKEN }}
          manifest-path: ./data-plane/slimrpc-compiler/Cargo.toml

      - uses: taiki-e/upload-rust-binary-action@v1
        with:
          bin: protoc-gen-slimrpc-go
          target: ${{ matrix.platform.target }}
          tar: unix
          zip: windows
          token: ${{ secrets.GITHUB_TOKEN }}
          manifest-path: ./data-plane/slimrpc-compiler/Cargo.toml
